drop database if exists dbalumnos;

CREATE DATABASE dbalumnos;
USE dbalumnos;

CREATE TABLE `alumnos` (
  `id_alumno` int PRIMARY KEY not null auto_increment,
  `nombre` varchar(255) not null,
  `apellido` varchar(255) not null,
  `correo` varchar(255)
);

CREATE TABLE `materias` (
  `id_materia` int PRIMARY KEY not null auto_increment,
  `nombre_materia` varchar(255) not null,
  `inscriptos` varchar(255) not null,
  `capacidad` int not null
);

INSERT INTO alumnos(nombre,apellido,correo)
VALUES
('Lucia','Dominguez','ldmominguez@escuela.com'),
('Nicolas','Perez','nperez@escuela.com'),
('Damian','Garcia','dgarcia@escuela.com'),
('Roxana','Paez','rpaez@escuela.com'),
('Manuela','Larroque','mlarroque@escuela.com');

INSERT INTO materias(nombre_materia,capacidad)
VALUES
('Biologia',10),
('Matematica',8),
('Lengua',5);
